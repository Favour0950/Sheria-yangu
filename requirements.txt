# Core dependencies
Flask>=3.0.0
requests>=2.32.0
python-dotenv>=1.0.0

# Bill scraper
beautifulsoup4>=4.12.0
pdfplumber>=0.11.0

# SMS (OTP + notifications)
africastalking>=1.2.9

# Database (local Postgres now, DigitalOcean droplet Postgres later — same schema)
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
